# End-to-end-Medical-Chatbot-Generative-AI

This project is an AI-powered medical question-answering system built using Retrieval-Augmented Generation (RAG). Medical documents are processed, split into semantic chunks, converted into vector embeddings, and stored in Pinecone. When a user asks a question, the system retrieves the most relevant document sections and uses a Groq LLM to generate
